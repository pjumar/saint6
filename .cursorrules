# Saint 6 Studio - Cursor Rules

## Code Style & Comments

### Comments
- **Minimum comments**: Only add comments when code is not self-explanatory
- Avoid obvious comments that restate what the code does
- Use comments for:
  - Complex business logic explanations
  - Non-obvious workarounds or hacks
  - API contracts or external dependencies
  - TODO/FIXME items with context
- Prefer self-documenting code through:
  - Descriptive variable and function names
  - Clear component structure
  - TypeScript types and interfaces

### Code Organization
- Keep functions small and focused (single responsibility)
- Extract complex logic into separate functions or utilities
- Group related functionality together
- Use meaningful names that describe intent

## TypeScript Rules

### Strict Type Safety
- Always use explicit types for function parameters and return types
- Avoid `any` - use `unknown` if type is truly unknown, then narrow it
- Use `as const` for literal types when appropriate
- Prefer interfaces for object shapes, types for unions/intersections
- Use type guards for runtime type checking

### Type Definitions
```typescript
// ✅ Good
interface User {
  id: string;
  name: string;
  email: string;
}

function getUser(id: string): Promise<User> {
  // ...
}

// ❌ Bad
function getUser(id: any): any {
  // ...
}
```

### Type Inference
- Let TypeScript infer types when obvious (e.g., `const count = 0`)
- Explicitly type function parameters and return values
- Use type assertions sparingly and only when necessary

### Import/Export
- Use named exports for components and utilities
- Use default exports only for pages and layouts (Next.js convention)
- Group imports: external → internal → types
- Use type-only imports when appropriate: `import type { User } from './types'`

## React Best Practices

### Component Structure
- Use functional components only (no class components)
- Keep components small and focused
- Extract reusable logic into custom hooks
- Use composition over inheritance

### Hooks Rules
- Follow Rules of Hooks (only call at top level)
- Use `useCallback` for functions passed to child components
- Use `useMemo` for expensive computations
- Extract complex state logic into custom hooks

### Component Patterns
```typescript
// ✅ Good - Functional component with proper typing
interface ButtonProps {
  label: string;
  onClick: () => void;
  variant?: 'primary' | 'secondary';
}

export function Button({ label, onClick, variant = 'primary' }: ButtonProps) {
  return (
    <button onClick={onClick} data-variant={variant}>
      {label}
    </button>
  );
}

// ❌ Bad - Missing types, inline styles, unclear structure
export function Button(props: any) {
  return <button style={{color: 'red'}} onClick={props.onClick}>{props.label}</button>;
}
```

### State Management
- Use `useState` for local component state
- Use `useReducer` for complex state logic
- Consider Context API for shared state (avoid prop drilling)
- Keep state as close to where it's used as possible

### Performance
- Use `React.memo` for components that re-render frequently
- Avoid creating objects/functions in render (use `useMemo`/`useCallback`)
- Lazy load heavy components with `React.lazy` and `Suspense`
- Optimize images with Next.js `Image` component

## Next.js Best Practices

### File Structure
- Follow Next.js App Router conventions
- Use `page.tsx` for routes
- Use `layout.tsx` for shared layouts
- Use `loading.tsx` for loading states
- Use `error.tsx` for error boundaries
- Use `not-found.tsx` for 404 pages

### Server vs Client Components
- Default to Server Components (no 'use client' directive)
- Use 'use client' only when needed:
  - Interactive components (onClick, onChange, etc.)
  - Browser APIs (localStorage, window, etc.)
  - Hooks (useState, useEffect, etc.)
  - Third-party libraries requiring client-side

### Data Fetching
- Use Server Components for data fetching when possible
- Use `async/await` in Server Components
- Use `fetch` with proper caching strategies
- Use `generateStaticParams` for static routes
- Use `generateMetadata` for SEO
- **Prefer Incremental Static Regeneration (ISG) over Server-Side Rendering**
  - Use `revalidate` option in fetch: `fetch(url, { next: { revalidate: 3600 } })`
  - Export `revalidate` constant from page: `export const revalidate = 3600`
  - Use ISG for pages that can tolerate slightly stale data
  - Only use dynamic rendering (`export const dynamic = 'force-dynamic'`) when absolutely necessary
  - Default to static generation with ISG for better performance and SEO

### Routing
- Use file-based routing (App Router)
- Use dynamic routes with `[slug]` or `[...slug]`
- Use route groups `(group)` for organization without affecting URL
- Use parallel routes `@folder` when needed

### Styling
- Use CSS Modules or Tailwind CSS (as configured)
- Use CSS variables from `globals.css` for design tokens
- Use utility classes for typography (`.heading-desktop`, `.body-regular`, etc.)
- Keep styles co-located with components when using CSS Modules
- **ALWAYS use `rem` units instead of `px`** for all sizing (width, height, padding, margin, font-size, etc.)
- Base font size is 16px (1rem = 16px), so divide px values by 16 to get rem
- Exception: Use `px` only for border-width (1px, 2px) and very small values where rem would be impractical
- **ALWAYS use mobile-first CSS approach**
  - Base styles (outside media queries) should be for mobile/small screens
  - Use `@media (min-width: ...)` queries to progressively enhance for larger screens
  - Never use `@media (max-width: ...)` queries - convert them to base styles instead
  - Breakpoint for desktop: `48.0625rem` (769px)

### Performance Optimization
- Use `next/image` for all images
- Implement proper loading states
- Use `next/dynamic` for code splitting
- Optimize fonts with `next/font`
- Use `generateStaticParams` for static generation
- **Prefer Incremental Static Regeneration (ISG) over Server-Side Rendering (SSR)**
  - Use `revalidate` option in `fetch` or `generateStaticParams` for ISG
  - Default to static generation with `revalidate` for pages that don't need real-time data
  - Only use Server Components for data fetching when ISG is not suitable
  - Use ISG pattern: `export const revalidate = 3600` (revalidate every hour) or use `revalidate` in fetch options

### API Routes
- Use Route Handlers in `app/api/` directory
- Return proper HTTP status codes
- Handle errors gracefully
- Validate request data
- Use TypeScript for request/response types

## Code Quality

### Error Handling
- Always handle errors explicitly
- Use try-catch for async operations
- Provide meaningful error messages
- Use error boundaries for React components
- Log errors appropriately (don't expose sensitive info)

### Testing Considerations
- Write testable code (pure functions when possible)
- Keep business logic separate from UI
- Use dependency injection for testability
- Consider edge cases and error states

### Accessibility
- Use semantic HTML elements
- Include proper ARIA labels when needed
- Ensure keyboard navigation works
- Maintain proper heading hierarchy
- Test with screen readers

### Security
- Never expose API keys or secrets in client code
- Validate and sanitize user input
- Use environment variables for sensitive data
- Implement proper authentication/authorization
- Use HTTPS in production

## Project-Specific Rules

### Design System
- Use CSS variables from `globals.css` for colors and typography
- Follow the typography utility classes (`.heading-desktop`, `.body-regular`, etc.)
- Maintain consistency with Figma design tokens
- Use responsive typography classes (mobile/desktop variants)

### Fonts
- Use `Public Sans` for body text
- Use `JetBrains Mono` for captions
- PP Fragment font will be added via @font-face when available
- Always use font variables: `var(--font-heading)`, `var(--font-body)`, `var(--font-mono)`

### Colors
- **ALWAYS use CSS variables for colors**: `var(--color-primary)`, `var(--color-text-primary)`, `var(--color-text-invert-100)`, etc.
- **NEVER hardcode color values** like `#FFFFFF`, `rgba(255, 255, 255, 0.8)`, or any hex/rgb/hsl values directly in CSS
- All colors must reference variables defined in `globals.css`
- Maintain color consistency across the application
- If a color variable doesn't exist, add it to `globals.css` first, then use it

## General Guidelines

### Code Formatting
- Use Biome for formatting (already configured)
- Run `pnpm format` before committing
- Use 2 spaces for indentation
- Use single quotes for strings (if configured)
- Add trailing commas in multi-line objects/arrays

### Git Practices
- Write clear, descriptive commit messages
- Keep commits focused (one logical change per commit)
- Use conventional commit format when possible

### Documentation
- Keep README.md updated
- Document complex algorithms or business logic
- Update type definitions when APIs change
- Comment public APIs and exported functions

### Testing & Browser Automation
- **Playwright MCP**: Playwright MCP is already installed for Cursor
- Use Playwright MCP tools to compare implementations with Figma designs
- Use browser automation tools to verify visual consistency
- Take screenshots and compare with design mockups when needed

### Design Implementation
- **ALWAYS use Figma MCP to check actual design colors, spacing, and styles** - Never guess colors or design values
- Use `mcp_Figma_get_design_context` to get exact color values, font sizes, spacing, and other design tokens from Figma
- Extract exact rgba/hex color values from Figma design context before implementing
- Verify all design values match Figma specifications exactly
- If design values are unclear, use Figma MCP to inspect specific nodes rather than guessing

### Component Refactoring & Organization
- **Always separate components into reusable, focused modules**
- Extract components into `app/components/` directory with clear naming
- Each component should have its own directory: `ComponentName/ComponentName.tsx` and `ComponentName/ComponentName.module.css`
- Keep components small and focused on a single responsibility
- Extract reusable UI elements (buttons, inputs, etc.) into separate components
- Extract complex sections (hero, gallery, etc.) into their own components
- Use TypeScript interfaces for all component props
- Keep page components (`page.tsx`) clean - they should only compose sections/components
- Group related components in subdirectories (e.g., `components/hero-section/`, `components/gallery-section/`)
- Extract shared logic into custom hooks when needed
- Use CSS Modules for component-specific styles
- Keep global styles minimal - prefer component-scoped styles

### SEO Best Practices
- **Always implement proper SEO metadata**
  - Use `generateMetadata` function in page components for dynamic metadata
  - Export `metadata` object from layout.tsx and page.tsx for static metadata
  - Include title, description, and Open Graph tags for all pages
  - Use descriptive, keyword-rich titles (under 60 characters)
  - Write compelling meta descriptions (150-160 characters)
  - Include Open Graph tags (`og:title`, `og:description`, `og:image`, `og:url`)
  - Add Twitter Card metadata when applicable
  - Set proper `lang` attribute on `<html>` tag
- **Semantic HTML Structure**
  - Use proper heading hierarchy (h1 → h2 → h3)
  - Use semantic HTML elements (`<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`)
  - Include alt text for all images
  - Use descriptive link text (avoid "click here")
- **Performance & SEO**
  - Use Next.js Image component with proper `alt` attributes
  - Implement proper loading states to improve Core Web Vitals
  - Use `next/font` for font optimization
  - Minimize JavaScript bundle size
  - Use `generateStaticParams` for dynamic routes to enable static generation
  - Prefer ISG (Incremental Static Regeneration) for better SEO and performance
- **Structured Data**
  - Consider adding JSON-LD structured data for rich snippets when applicable
  - Use schema.org markup for business information, articles, etc.
- **URL Structure**
  - Use clean, descriptive URLs
  - Implement proper canonical URLs
  - Use proper 301 redirects for moved content
- **Accessibility & SEO**
  - Ensure proper ARIA labels for interactive elements
  - Maintain proper heading hierarchy for screen readers
  - Use semantic HTML which benefits both accessibility and SEO

## Anti-Patterns to Avoid

- ❌ Don't use `any` type
- ❌ Don't create components with inline styles (use CSS/Tailwind)
- ❌ Don't fetch data in client components when Server Components can do it
- ❌ Don't ignore TypeScript errors
- ❌ Don't commit console.log statements
- ❌ Don't use `var` (use `const` or `let`)
- ❌ Don't mutate props or state directly
- ❌ Don't create unnecessary wrapper components
- ❌ Don't ignore accessibility requirements
- ❌ Don't hardcode values that should be configurable
- ❌ **Don't hardcode color values** - Always use CSS variables (e.g., `var(--color-text-invert-100)` instead of `#FFFFFF`)

