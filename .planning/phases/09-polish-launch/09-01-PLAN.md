---
phase: 09-polish-launch
plan: 01
type: execute
---

<objective>
Remove debug artifacts and extract duplicated transformer functions into shared utilities.

Purpose: Clean up the major code review finding (debug code in production) and reduce maintenance burden from duplicated transformer logic across 5+ service pages.
Output: Clean Creative page, shared `app/lib/transformers.ts` module, all service pages updated to use shared transformers.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/[locale]/creative/page.tsx
@app/components/debug-panel/DebugPanel.tsx
@app/[locale]/set-design/page.tsx
@app/[locale]/production/page.tsx
@app/[locale]/event-planning/page.tsx
@app/[locale]/decor/page.tsx
@app/[locale]/page.tsx
@app/lib/strapi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove debug code from Creative page and delete DebugPanel</name>
  <files>app/[locale]/creative/page.tsx, app/components/debug-panel/DebugPanel.tsx</files>
  <action>
In `app/[locale]/creative/page.tsx`:
- Remove the `DebugPanel` import (from `@/app/components/debug-panel/DebugPanel`)
- Remove the `debugInfo` object construction (the block that builds `{ page, sections, timestamp, locale, env }`)
- Remove all `console.log` statements (lines ~178-194, the 17 lines of debug logging)
- Remove the `<DebugPanel info={debugInfo} />` JSX usage
- Keep all other page logic intact — the CMS fetch, transformers, and component rendering are all valid

Delete `app/components/debug-panel/DebugPanel.tsx` entirely — it is only used on the Creative page and is a debug artifact.

Also check if `app/components/debug-panel/` directory has any other files. If DebugPanel.tsx is the only file, delete the entire directory.
  </action>
  <verify>`npx biome check app/[locale]/creative/page.tsx` passes with no errors. Grep for "DebugPanel" across the codebase returns zero results. Grep for "console.log" in creative/page.tsx returns zero results.</verify>
  <done>Creative page has no debug logging, no DebugPanel import/usage. DebugPanel component and its directory are deleted. Page still renders correctly with all CMS data.</done>
</task>

<task type="auto">
  <name>Task 2: Extract shared transformer functions into app/lib/transformers.ts</name>
  <files>app/lib/transformers.ts, app/[locale]/creative/page.tsx, app/[locale]/set-design/page.tsx, app/[locale]/production/page.tsx, app/[locale]/event-planning/page.tsx, app/[locale]/decor/page.tsx, app/[locale]/page.tsx</files>
  <action>
Create `app/lib/transformers.ts` with shared transformer functions extracted from service pages. The following are duplicated:

1. **transformWorkflow** — exists in 5 files (creative:81, set-design:52, production:70, event-planning:75, decor:46). Extract the common implementation. It transforms Strapi workflow step data into component props (typically `{ number, title, description }`).

2. **transformPortfolio** — exists in 3 files (creative:100, set-design:71, decor:65). Transforms Strapi portfolio/gallery items into `{ image, title, category }` props.

3. **transformTestimonials** — exists in 2 files (creative:124, set-design:95). Transforms Strapi testimonial data into `{ quote, author, role, avatar }` props.

4. **transformKeyProjects** — exists in 2 files (page.tsx:161, production:89). Transforms Strapi project data into component props.

For each:
- Read ALL implementations first to identify any page-specific differences
- If implementations are truly identical, extract once and export
- If there are minor differences (e.g., different fallback images), parameterize them (e.g., accept a `fallbackImage` option)
- Use the `getStrapiImageUrl` import from `@/app/lib/strapi`
- Export each function as a named export
- Add the necessary type imports from strapi.ts or define local interfaces as needed

Then update each page file:
- Replace local transformer function definitions with imports from `@/app/lib/transformers`
- Keep page-specific transformers (e.g., `transformCreativeServices`, `transformEventProjects`, `transformProductionServices`, `transformClientLogos`) in their respective page files — these are unique to each page
- Do NOT move the Homepage-specific transformers (`transformBrandLogos`, `transformGalleryImages`, `transformSpaceSection`, `transformCrewArea`) — they are unique to the homepage

Be careful: each page's local transformers may have slightly different field mappings or fallback values. Compare before extracting. If differences exist, handle via optional parameters rather than maintaining separate copies.
  </action>
  <verify>`npm run build` succeeds with no errors. `npx biome check app/lib/transformers.ts` passes. Grep for `function transformWorkflow` returns only `app/lib/transformers.ts`. Grep for `function transformPortfolio` returns only `app/lib/transformers.ts`. Grep for `function transformTestimonials` returns only `app/lib/transformers.ts`.</verify>
  <done>Shared transformers extracted to `app/lib/transformers.ts`. All 5 service pages import from shared module. No duplicate transformer definitions remain (except page-specific ones). Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Fix OG image URL to use absolute path</name>
  <files>app/lib/seo.ts, .env.local, .env.example</files>
  <action>
In `app/lib/seo.ts`, the `FALLBACK_SEO.og_image` is `"/og-image.jpg"` — a relative path. OpenGraph requires absolute URLs for images.

Fix by:
1. Add a `SITE_URL` constant at the top of seo.ts: `const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://saint6.studio";`
2. In `buildPageMetadata`, when constructing the OpenGraph images array, prefix `imageUrl` with `SITE_URL` if it starts with `/` (is relative). Example: `const absoluteImageUrl = imageUrl.startsWith("/") ? `${SITE_URL}${imageUrl}` : imageUrl;`
3. Use `absoluteImageUrl` in the `openGraph.images` array

Also ensure `NEXT_PUBLIC_SITE_URL` is documented. If `.env.example` exists, add it there. If not, add a comment in seo.ts noting the env var.

Do NOT change the metadataBase — Next.js handles that separately.
  </action>
  <verify>Read seo.ts and confirm `imageUrl` is always absolute when used in OpenGraph. `npx biome check app/lib/seo.ts` passes.</verify>
  <done>OG image URLs are always absolute. Relative fallback `/og-image.jpg` gets prefixed with site URL. Both CMS-provided and fallback images resolve to absolute URLs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors or warnings
- [ ] No `console.log` in creative/page.tsx
- [ ] No `DebugPanel` references anywhere in codebase
- [ ] `transformWorkflow` defined only in `app/lib/transformers.ts`
- [ ] `transformPortfolio` defined only in `app/lib/transformers.ts`
- [ ] `transformTestimonials` defined only in `app/lib/transformers.ts`
- [ ] OG image URL in seo.ts resolves to absolute URL
- [ ] `npx biome check` passes
</verification>

<success_criteria>

- Debug code fully removed from Creative page
- DebugPanel component deleted
- Shared transformers extracted — no duplicate transformer definitions across pages
- OG image always resolves to absolute URL
- Build passes, no new errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-launch/09-01-SUMMARY.md`
</output>
