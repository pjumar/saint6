---
phase: 09-polish-launch
plan: 03
type: execute
---

<objective>
Verify production build, audit SEO metadata across all pages, and prepare for deployment.

Purpose: Final verification that everything works end-to-end before launch — build succeeds, SEO is correct, and the project state is updated to mark completion.
Output: Verified production build, SEO audit results, updated project state marking v1.1 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/lib/seo.ts
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build verification</name>
  <files>None (verification only)</files>
  <action>
Run `npm run build` and verify:
1. All 9 pages build successfully under the `[locale]` route group (both `/en` and `/vi` variants)
2. No TypeScript errors
3. No build warnings (other than known Next.js informational messages)
4. ISR pages show correct revalidation interval (60s) in build output
5. Static pages are properly generated

Check the build output for:
- Total page count matches expected (9 pages × 2 locales = 18 routes minimum)
- No "Failed to compile" errors
- No "Module not found" errors
- Bundle sizes are reasonable (no unexpectedly large chunks)

If build fails, identify the issue and fix it before proceeding. Common issues after refactoring:
- Missing imports (from transformer extraction in 09-01)
- Changed export names (from fallback data split in 09-02)
- Type errors from translation type changes (from 09-02)

Also verify that the double-fetch pattern in `generateMetadata` is handled by Next.js fetch deduplication: check build logs for any indication of duplicate CMS requests. If Next.js 16 doesn't deduplicate these, consider using `React.cache()` to wrap the fetch functions. But do NOT change anything if the build is clean — this is verification only unless a problem is found.
  </action>
  <verify>`npm run build` exits with code 0. Build output shows all pages generated.</verify>
  <done>Production build succeeds with zero errors and zero warnings. All 18+ routes generated. ISR configured correctly.</done>
</task>

<task type="auto">
  <name>Task 2: SEO metadata audit across all pages</name>
  <files>app/[locale]/page.tsx, app/[locale]/studio-rental/page.tsx, app/[locale]/creative/page.tsx, app/[locale]/production/page.tsx, app/[locale]/set-design/page.tsx, app/[locale]/event-planning/page.tsx, app/[locale]/decor/page.tsx, app/[locale]/about/page.tsx, app/[locale]/contact/page.tsx, app/[locale]/layout.tsx, app/lib/seo.ts</files>
  <action>
Audit all 9 pages for correct SEO metadata by reading each page's `generateMetadata` function:

For each page, verify:
1. **Title** — Page has a unique, descriptive title (not generic or missing). Check both the `title` field and the `openGraph.title`
2. **Description** — Page has a meta description (at least 50 chars, under 160)
3. **OpenGraph image** — After 09-01 fix, verify the OG image URL resolves to an absolute URL. Check that `buildPageMetadata` in seo.ts correctly prefixes relative URLs
4. **Locale alternates** — Each page should have `alternates.languages` pointing to both `/en/...` and `/vi/...` variants. Check if this is handled in `buildPageMetadata` or in `layout.tsx`
5. **Canonical URL** — Check if canonical URLs are set (not strictly required but good practice)

Create a brief SEO audit summary as a comment in the SUMMARY.md showing:
- Page name | Title present | Description present | OG image absolute | Locale alternates

If any page is missing metadata, add it using the `buildPageMetadata` utility from `seo.ts`. Most pages should already have this from Phase 8 CMS integration.

Do NOT add structured data (JSON-LD) or schema.org markup — that's out of scope for v1.1.
  </action>
  <verify>Read each page's generateMetadata export and confirm all 5 checks pass. `npm run build` succeeds (metadata is validated at build time).</verify>
  <done>All 9 pages have title, description, and OG image metadata. OG images use absolute URLs. Locale alternates configured for EN/VI.</done>
</task>

<task type="auto">
  <name>Task 3: Update project state to mark v1.1 complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Update project tracking files to reflect Phase 9 and v1.1 completion:

1. In `.planning/ROADMAP.md`:
   - Mark 09-01, 09-02, 09-03 as complete with today's date
   - Update Phase 9 row in the Progress table: `3/3 | Complete | [today's date]`
   - Update the v1.1 milestone status from "In Progress" to "COMPLETE" with date

2. In `.planning/STATE.md`:
   - Update "Phase: 9 of 9" to show "Complete"
   - Update "Plan: 3 of 3 in current phase"
   - Set progress to 100% (22 of 22 plans complete)
   - Update milestones table: v1.1 Production Ready → COMPLETE with date
   - Update "Last activity" to today's date
   - Update Phase 9 row in the By Phase table
   - Clear "Next Steps" — project is complete
   - Add final note that v1.1 is shipped

Keep the accumulated context and decisions — they serve as project history.
  </action>
  <verify>Read STATE.md and confirm progress shows 100%. Read ROADMAP.md and confirm all phases marked complete.</verify>
  <done>STATE.md shows 100% progress, all phases complete. ROADMAP.md has all 22 plans marked done. v1.1 milestone marked COMPLETE.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All 9 pages have SEO metadata (title, description, OG image)
- [ ] OG images resolve to absolute URLs
- [ ] STATE.md shows 100% progress
- [ ] ROADMAP.md shows all phases complete
- [ ] `npx biome check` passes
</verification>

<success_criteria>

- Production build passes with zero errors
- All pages have complete SEO metadata
- Project state accurately reflects completion
- v1.1 milestone marked COMPLETE in both STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-launch/09-03-SUMMARY.md`
</output>
