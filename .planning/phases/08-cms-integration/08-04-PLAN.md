---
phase: 08-cms-integration
plan: 04
type: execute
---

<objective>
Wire About and Contact pages to Strapi with static generation, completing Phase 8.

Purpose: Integrate the final two pages using ISG pattern and verify complete static site generation.
Output: All pages statically generated with Strapi data at build time, Phase 8 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-cms-integration/08-01-SUMMARY.md
@.planning/phases/08-cms-integration/08-02-SUMMARY.md
@.planning/phases/08-cms-integration/08-03-SUMMARY.md

**Strapi types and functions:**
@app/lib/strapi.ts

**Pages to integrate:**
- app/[locale]/about/page.tsx (or about-us)
- app/[locale]/contact/page.tsx (or contact-us)

**Key Strapi functions:**
- `getAboutPage(locale)` - hero, intro, vision, mission, values, story, timeline, founder
- `getContactPage(locale)` - hero, info, map_image

**ISG Pattern (established in 08-01, 08-02, 08-03):**
- Server Component fetches at BUILD TIME
- Data baked into static HTML
- No runtime CMS calls
- Client wrappers for interactive sections
- Dev fallback only for local development
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire About page to Strapi</name>
  <files>app/[locale]/about/page.tsx, app/lib/fallback-data.ts</files>
  <action>
    1. Locate About page (might be /about or /about-us)

    2. Remove "use client", add data fetching:
       ```typescript
       const pageData = await getAboutPage(locale);
       ```

    3. Move hardcoded data to fallback-data.ts:
       - `FALLBACK_ABOUT_INTRO`
       - `FALLBACK_ABOUT_VALUES`
       - `FALLBACK_ABOUT_STORY`
       - `FALLBACK_ABOUT_TIMELINE`
       - `FALLBACK_ABOUT_FOUNDER`

    4. Create data transformers:
       - `transformStrapiAboutIntro()`
       - `transformStrapiValue()`
       - `transformStrapiTimelineItem()`
       - `transformStrapiFounder()`

    5. Handle interactive sections:
       - Timeline may have animations
       - Values may have hover effects
       - Create client wrapper if needed

    6. Test both locales
  </action>
  <verify>
    - `pnpm build` succeeds
    - About page renders all sections
    - Timeline displays correctly
    - Values section displays correctly
    - Language switching works
  </verify>
  <done>
    - About page integrated with CMS
    - All sections use Strapi data or fallback
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Contact page to Strapi</name>
  <files>app/[locale]/contact/page.tsx, app/lib/fallback-data.ts</files>
  <action>
    1. Locate Contact page (might be /contact or /contact-us)

    2. Remove "use client", add data fetching:
       ```typescript
       const pageData = await getContactPage(locale);
       ```

    3. Move hardcoded data to fallback-data.ts:
       - `FALLBACK_CONTACT_INFO`
       - `FALLBACK_CONTACT_MAP`

    4. Create data transformers:
       - `transformStrapiContactInfo()`

    5. Handle contact form:
       - Contact form needs client-side state
       - Create ContactPageClient wrapper for form
       - Pass contact info from server to client

    6. Test both locales
  </action>
  <verify>
    - `pnpm build` succeeds
    - Contact page renders info section
    - Contact form works (validation, submission)
    - Map/location displays correctly
    - Language switching works
  </verify>
  <done>
    - Contact page integrated with CMS
    - Contact form interactive behavior works
  </done>
</task>

<task type="auto">
  <name>Task 3: Final ISG verification and Phase 8 completion</name>
  <files>Multiple files across app/</files>
  <action>
    1. Verify COMPLETE static generation:
       - Run `pnpm build`
       - ALL pages should show as statically generated in build output
       - Run `pnpm start`
       - Open Network tab - verify ZERO Strapi calls at runtime
       - Navigate through entire site - no CMS requests

    2. Full site verification (both locales):
       - Homepage: /en and /vi
       - Studio Rental: /en/studio-rental and /vi/studio-rental
       - Creative: /en/creative and /vi/creative
       - Production: /en/production and /vi/production
       - Set Design: /en/set-design and /vi/set-design
       - Event Planning: /en/event-planning and /vi/event-planning
       - Decor: /en/decor and /vi/decor
       - About: /en/about and /vi/about
       - Contact: /en/contact and /vi/contact

    3. Navigation and interactive features:
       - All header/footer links work
       - Language switcher works on all pages
       - Forms work (contact, inquiry)
       - Carousels, accordions, etc. work

    4. Final cleanup:
       - Search for remaining "Phase 8" or "CMS integration" TODOs
       - Remove unused imports
       - Verify all hardcoded data moved to fallback-data.ts

    5. Build verification:
       ```bash
       pnpm build
       pnpm lint
       ```
  </action>
  <verify>
    - `pnpm build` succeeds with ALL pages statically generated
    - `pnpm start` serves site with NO runtime Strapi calls
    - All 9+ pages render in both locales
    - Navigation and forms work
    - No CMS-related TODO comments remaining
  </verify>
  <done>
    - ALL pages statically generated with CMS data
    - NO runtime CMS dependency
    - Phase 8: CMS Integration complete
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds
- [ ] About page works in en and vi
- [ ] Contact page works in en and vi
- [ ] Contact form validation and submission works
- [ ] All 9+ pages verified working
- [ ] Navigation works throughout site
- [ ] Fallback data works when CMS unavailable
- [ ] No CMS-related TODO comments remaining
</verification>

<success_criteria>
- About and Contact pages fetch from Strapi
- All site pages now use CMS integration
- Fallback system provides resilience
- Phase 8: CMS Integration complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-cms-integration/08-04-SUMMARY.md`:

# Phase 8 Plan 4: About & Contact Pages + Final Verification Summary

**[Substantive one-liner about completing Phase 8]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [Files and their purposes]

## Decisions Made
- [Any final decisions]

## Issues Encountered
- [Problems and resolutions, or "None"]

## Phase 8 Complete
All pages now statically generated with Strapi data at BUILD TIME:
- Homepage
- Studio Rental
- Creative, Production, Set Design, Event Planning, Decor (5 service pages)
- About
- Contact

**ISG Benefits achieved:**
- Fast page loads (pre-rendered HTML)
- No runtime CMS dependency
- CDN-friendly static assets
- Content updates via rebuild

## Next Phase Readiness
Ready for Phase 9: Polish & Launch
- Social media links need updating
- Performance optimization
- SEO verification
- Deployment preparation
</output>
