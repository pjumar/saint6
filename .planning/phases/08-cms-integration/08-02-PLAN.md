---
phase: 08-cms-integration
plan: 02
type: execute
---

<objective>
Wire Studio Rental page to Strapi with static generation (ISG).

Purpose: Integrate the most complex service page using the ISG pattern - data fetched at BUILD TIME only, no runtime CMS dependency.
Output: Studio Rental page statically generated with Strapi data baked in.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-cms-integration/08-01-SUMMARY.md

**Strapi types and functions:**
@app/lib/strapi.ts

**Current Studio Rental page (hardcoded):**
@app/[locale]/studio-rental/page.tsx

**Dev fallback utilities (from 08-01):**
@app/lib/fallback-data.ts

**Key Strapi functions:**
- `getStudioRentalPage(locale)` - hero, intro, stats, rooms, facilities, equipment, faqs
- `getStudioRooms(type?)` - blank/concept rooms collection
- `getEquipmentItems()` - equipment collection
- `getFaqItems(category?)` - FAQ collection

**ISG Pattern (established in 08-01):**
- Server Component fetches at BUILD TIME
- Data baked into static HTML
- No runtime CMS calls
- Dev fallback only for local development
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Studio Rental to Server Component with data fetching</name>
  <files>app/[locale]/studio-rental/page.tsx, app/lib/fallback-data.ts</files>
  <action>
    1. Remove "use client" directive from page

    2. Fetch Strapi data:
       ```typescript
       const pageData = await getStudioRentalPage(locale);
       const blankRooms = await getStudioRooms('blank');
       const conceptRooms = await getStudioRooms('concept');
       const equipment = await getEquipmentItems();
       ```

    3. Add fallback data to `fallback-data.ts`:
       - Move `studioRooms`, `conceptRooms`, `equipmentItems` from page
       - Export as `FALLBACK_STUDIO_ROOMS`, `FALLBACK_CONCEPT_ROOMS`, `FALLBACK_EQUIPMENT`

    4. Create data transformation helpers:
       - `transformStrapiRoom(room: StrapiStudioRoom)` → RoomCard props
       - `transformStrapiEquipment(item: StrapiEquipmentItem)` → EquipmentGrid props

    5. Handle FAQ data:
       - FAQs currently use translations (t.STUDIO_RENTAL.FAQ)
       - If Strapi has FAQs, use them; else keep translation-based fallback
       - FAQs need locale-aware fetching
  </action>
  <verify>
    - `pnpm build` succeeds
    - `pnpm dev` shows Studio Rental page rendering
    - Rooms, equipment, facilities sections display correctly
  </verify>
  <done>
    - Studio Rental is Server Component
    - All sections use Strapi data or fallback
    - No hardcoded data in page file (moved to fallback-data.ts)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire interactive sections with client component wrappers</name>
  <files>app/[locale]/studio-rental/page.tsx, app/[locale]/studio-rental/StudioRentalClient.tsx</files>
  <action>
    1. Identify interactive sections needing client context:
       - FAQAccordion (uses state for expand/collapse)
       - ContactSection (uses form state)
       - StudioIntro CTA (if it has click handlers)

    2. Create `StudioRentalClient.tsx` wrapper:
       - "use client" directive
       - Receives all data as props from server page
       - Uses `useTranslation()` for translated strings
       - Renders all interactive sections

    3. Update server page to:
       - Fetch all data
       - Pass to StudioRentalClient as serializable props
       - Keep static sections (hero, stats) in server component if possible

    4. Verify translations still work:
       - FAQs use `t.STUDIO_RENTAL.FAQ.*`
       - Intro uses `t.STUDIO_RENTAL.INTRO.*`
       - These come from TranslationProvider in layout
  </action>
  <verify>
    - FAQ accordion expands/collapses correctly
    - Contact form validation works
    - All translations display in correct language
    - Language switching works (en ↔ vi)
  </verify>
  <done>
    - Interactive sections work via client wrapper
    - Data flows: Server fetch → Client render
    - Translations functioning correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify static generation and clean up</name>
  <files>app/[locale]/studio-rental/page.tsx</files>
  <action>
    1. Test all page sections render with CMS data:
       - Hero section with CMS background image
       - Intro with CMS text
       - Stats section
       - Room cards (blank and concept)
       - Full rental card
       - Facilities showcase
       - Equipment grid
       - FAQ accordion
       - Contact section

    2. Verify static generation:
       - Run `pnpm build`
       - Check build output shows static generation
       - Run `pnpm start` - verify NO runtime Strapi calls
       - Check Network tab - no Strapi requests after page load

    3. Clean up:
       - Remove TODO comments about "CMS integration in Phase 8"
       - Remove unused imports
       - Move hardcoded data to fallback-data.ts (dev only)
  </action>
  <verify>
    - `pnpm build` succeeds with static generation
    - `pnpm start` serves static pages (no runtime fetch)
    - All sections render correctly
    - Language switching works
  </verify>
  <done>
    - Studio Rental statically generated with CMS data
    - No runtime CMS dependency
    - ISG pattern validated
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without errors
- [ ] Studio Rental page renders all sections correctly
- [ ] FAQ accordion interactive behavior works
- [ ] Contact form works
- [ ] Language switching en ↔ vi works
- [ ] Fallback data works when Strapi unavailable
</verification>

<success_criteria>
- Studio Rental fetches from Strapi (or uses fallback)
- All interactive sections function correctly
- Server/Client component pattern established
- No hardcoded data remaining in page file
</success_criteria>

<output>
After completion, create `.planning/phases/08-cms-integration/08-02-SUMMARY.md`:

# Phase 8 Plan 2: Studio Rental CMS Integration Summary

**[Substantive one-liner about what was accomplished]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [Files and their purposes]

## Decisions Made
- [Any decisions about component structure, fallback strategy, etc.]

## Issues Encountered
- [Problems and resolutions, or "None"]

## Next Step
Ready for 08-03-PLAN.md (Service Pages CMS Integration)
</output>
