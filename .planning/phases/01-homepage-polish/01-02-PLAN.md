<?xml version="1.0" encoding="UTF-8"?>
<plan>
  <metadata>
    <phase>1</phase>
    <plan_number>02</plan_number>
    <title>Update Hero Section, Header, and Layout to Match Design</title>
    <objective>
      Implement visual refinements identified in Plan 01-01 gap analysis. Update hero section styling, header navigation layout, and main page layout structure to precisely match Figma design specifications. Focus on typography, spacing, colors, and structural layout.
    </objective>
    <depends_on>
      Plan 01-01 completed: Design audit and gap analysis available in .planning/phases/01-homepage-polish/01-GAPS.md
    </depends_on>
    <context>
      <discovery_level>0 (Execution) - Gaps already identified, ready to implement</discovery_level>
      <codebase_state>
        - Components to modify: HeroSection, Header, MenuOverlay, page layout wrapper
        - Styling approach: CSS Modules + Tailwind utility classes
        - No breaking changes expected, only refinement
        - All components already exist, no new components needed
      </codebase_state>
      <key_concerns>
        - Maintain responsive design while updating desktop layout
        - Preserve animation behavior in HeroSection
        - Don't break mobile menu functionality in Header
        - CSS changes should be scoped to component modules (no global side effects)
      </key_concerns>
    </context>
  </metadata>

  <execution_context>
    <tools_available>
      - Read/Edit/Write for component and CSS files
      - Bash for dev server testing (npm run dev)
      - Figma MCP reference for checking pixel-perfect details if needed
    </tools_available>
    <resources>
      - Gap analysis: .planning/phases/01-homepage-polish/01-GAPS.md (from Plan 01-01)
      - Components: app/components/{hero-section,header,menu-overlay}/
      - Styles: app/components/{hero-section,header}//*.module.css, app/page.module.css
      - Current implementation: app/[locale]/page.tsx
    </resources>
  </execution_context>

  <tasks>
    <task>
      <number>1</number>
      <title>Update Hero Section Styling &amp; Structure</title>
      <description>
        Modify HeroSection component and CSS to match design:

        Files to update:
        - app/components/hero-section/HeroSection.tsx (if structure needs changes)
        - app/components/hero-section/HeroSection.module.css (styling updates)
        - app/components/header/Header.tsx (if part of hero)
        - app/components/header/Header.module.css

        Changes likely needed:
        - Adjust hero container padding/margins to match design spacing
        - Update headline typography (font size, weight, line height)
        - Refine subheading styling if different
        - Adjust button styling, spacing, and sizing
        - Background image positioning/sizing if design has changed
        - Hero section height and viewport coverage
        - Text color and any gradient overlays
        - Mobile vs desktop layout adjustments

        Use CSS Modules for component-scoped changes. Only modify Tailwind utilities if absolutely necessary.
      </description>
      <acceptance_criteria>
        - [ ] HeroSection matches Figma design for desktop view
        - [ ] Typography matches (font sizes, weights, line heights)
        - [ ] Spacing matches (padding, margins, gaps)
        - [ ] Colors match design specification
        - [ ] Background/imagery positioned correctly
        - [ ] Button styling and placement correct
        - [ ] Mobile responsive behavior preserved
        - [ ] No layout shift or unintended side effects
        - [ ] Dev server runs without errors: npm run dev
      </acceptance_criteria>
      <checkpoint>
        Verify with npm run dev - HeroSection should match design when viewed at desktop and mobile widths
      </checkpoint>
    </task>

    <task>
      <number>2</number>
      <title>Update Header Navigation &amp; Styling</title>
      <description>
        Refine Header component layout and styling:

        Files to update:
        - app/components/header/Header.tsx (if layout needs restructuring)
        - app/components/header/Header.module.css (styling refinements)
        - app/components/language-selector/LanguageSelector.tsx &amp; .module.css (if styling needed)
        - app/components/hamburger-menu/HamburgerMenu.tsx &amp; .module.css (if styling needed)

        Changes likely needed:
        - Logo sizing and positioning
        - Navigation menu layout (horizontal spacing, alignment)
        - Language selector styling and position
        - Hamburger menu button styling (size, color, spacing)
        - Header height and padding
        - Background styling (solid, transparent, or gradient)
        - Mobile menu trigger point / responsive breakpoint
        - Header fixed vs relative positioning if design specifies

        Preserve existing functionality:
        - Mobile menu toggle still works
        - Language selector still switches locales
        - Navigation links still route correctly
      </description>
      <acceptance_criteria>
        - [ ] Header layout matches Figma design desktop view
        - [ ] Logo size and position correct
        - [ ] Navigation menu spacing and alignment match
        - [ ] Language selector properly styled and positioned
        - [ ] Hamburger menu appears at correct breakpoint
        - [ ] Header colors and backgrounds match
        - [ ] Mobile responsive behavior correct
        - [ ] No broken functionality
        - [ ] Dev server runs without errors
      </acceptance_criteria>
      <checkpoint>
        Verify npm run dev - Header should match design at both desktop (nav visible) and mobile (hamburger visible)
      </checkpoint>
    </task>

    <task>
      <number>3</number>
      <title>Update Main Page Layout &amp; Section Spacing</title>
      <description>
        Refine app/page.module.css and overall page structure:

        Files to update:
        - app/page.module.css (main layout wrapper styles)
        - app/[locale]/page.tsx (if structure needs adjustment)

        Changes likely needed:
        - Homepage wrapper max-width and centering
        - Content container padding and margins
        - Section spacing (gaps between sections)
        - Background colors/gradients for page sections
        - Overall page width constraints
        - Container alignment and gutters for responsiveness

        This might include:
        - TrustedBySection spacing adjustments (inherited from parent container)
        - GallerySection spacing adjustments
        - KeyProjectSection spacing adjustments
        - Footer spacing (but Footer component owns its own styling)

        Note: Don't modify individual section component CSS in this task, just the overall page wrapper.
      </description>
      <acceptance_criteria>
        - [ ] Page layout matches design overall spacing
        - [ ] Sections properly spaced vertically
        - [ ] Content container width and centering correct
        - [ ] Padding/margins align with design specification
        - [ ] Responsive behavior maintains proportions
        - [ ] No horizontal scroll at any breakpoint
        - [ ] Background colors/sections render correctly
        - [ ] Dev server shows correct layout
      </acceptance_criteria>
      <checkpoint>
        Verify npm run dev - full page layout should match Figma from top to bottom
      </checkpoint>
    </task>

    <task>
      <number>4</number>
      <title>Visual Verification &amp; Browser Testing</title>
      <description>
        Test changes across multiple viewports to ensure design accuracy:

        Testing steps:
        1. Run dev server: npm run dev
        2. Open http://localhost:3000/en in browser
        3. Test at multiple viewport widths:
           - Mobile: 375px (iPhone)
           - Tablet: 768px (iPad)
           - Desktop: 1440px (wide screen)
           - Desktop: 1920px (very wide)

        4. Verify for each viewport:
           - Layout matches Figma mockup
           - Text readable and properly sized
           - Images display at correct aspect ratios
           - Spacing looks correct (no cramped or excessive whitespace)
           - No layout shifts when scrolling
           - Buttons/interactive elements properly spaced

        5. Check responsive transitions:
           - Navigation switches from menu to hamburger correctly
           - Layouts adapt smoothly at breakpoints
           - Images scale appropriately

        6. Scroll through full page:
           - Hero section animates if applicable
           - TrustedBySection animations work
           - Gallery displays correctly
           - KeyProjectSection shows content properly
           - Footer renders at bottom

        Document any remaining issues or pixel-perfect tweaks needed.
      </description>
      <acceptance_criteria>
        - [ ] Desktop layout (1440px) matches Figma design
        - [ ] Mobile layout (375px) matches Figma design
        - [ ] Tablet layout (768px) matches Figma design
        - [ ] All text is readable and properly sized
        - [ ] Images display with correct aspect ratios
        - [ ] No layout shift or scroll jank
        - [ ] Animations work smoothly
        - [ ] No console errors during browsing
        - [ ] All interactive elements (buttons, menu) functional
      </acceptance_criteria>
      <checkpoint>
        Browser screenshots at multiple breakpoints showing final result
      </checkpoint>
    </task>
  </tasks>

  <verification>
    <manual_steps>
      1. Visual comparison: Side-by-side Figma design vs browser at multiple widths
      2. Pixel-perfect check for typography and spacing
      3. Test all interactive elements work correctly
      4. Verify animations are smooth and on-brand
      5. Check mobile experience is polished
    </manual_steps>
    <automated_checks>
      - [ ] npm run build completes without errors
      - [ ] npm run dev runs without console errors or warnings
      - [ ] TypeScript compilation successful (no type errors)
      - [ ] Biome linter passes (npm run lint)
    </automated_checks>
  </verification>

  <success_criteria>
    - User reviews updated homepage and confirms it matches Figma design
    - No visual regressions from previous version
    - Responsive design works at all specified breakpoints
    - All interactive elements function correctly
    - Build and dev server run cleanly
    - Ready to move to Plan 01-03 for animation/responsive verification
  </success_criteria>

  <output>
    Deliverables:
    1. Updated HeroSection component (app/components/hero-section/HeroSection.module.css)
    2. Updated Header component (app/components/header/Header.module.css)
    3. Updated page layout (app/page.module.css)
    4. Verification notes documenting visual testing results
  </output>

  <notes>
    - Start with the most impactful changes from gap analysis (typography, spacing)
    - Use CSS Modules to keep changes scoped and maintainable
    - Test frequently with dev server to catch issues early
    - If design has animations or interactions, those are handled in Plan 01-03
    - Keep Git history clean: commit by component/section as you complete
  </notes>
</plan>
