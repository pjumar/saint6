---
phase: 07-error-handling
plan: 01
type: execute
---

<objective>
Add error boundary component to prevent app crashes when components throw errors.

Purpose: Production-ready stability - any component error should show fallback UI instead of white screen crash.
Output: ErrorBoundary component wrapping the app, with user-friendly fallback UI.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Codebase context
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

# Key files to modify
@app/[locale]/layout.tsx

**From CONCERNS.md:**
- "No error boundary component exists"
- "Any component error crashes entire application (white screen)"
- "TranslationContext throws if useTranslation() called without provider"
- Priority: HIGH (affects availability)

**Established patterns:**
- Components in `app/components/{feature-name}/` with PascalCase
- CSS Modules paired with components
- Named exports (not default)
- Props interface: `ComponentNameProps`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ErrorBoundary component</name>
  <files>app/components/error-boundary/ErrorBoundary.tsx, app/components/error-boundary/ErrorBoundary.module.css, app/components/error-boundary/index.ts</files>
  <action>
Create a React error boundary class component (error boundaries must be class components - cannot use hooks).

Structure:
- ErrorBoundary.tsx: Class component extending React.Component
- Props: children, fallback (optional custom fallback)
- State: hasError, error (for display)
- Methods: static getDerivedStateFromError(), componentDidCatch() for logging
- Fallback UI: Centered message with "Something went wrong" heading, error message (dev only), and "Reload page" button

Fallback UI styling:
- Full viewport height centered
- Clean, minimal design matching site aesthetics
- Button uses existing Button component from ui/
- Hide error details in production (check process.env.NODE_ENV)

Export pattern: Named export `ErrorBoundary` via index.ts barrel file.
  </action>
  <verify>TypeScript compiles without errors: `pnpm tsc --noEmit`</verify>
  <done>ErrorBoundary component exists with fallback UI, catches errors via getDerivedStateFromError</done>
</task>

<task type="auto">
  <name>Task 2: Wrap app layout with ErrorBoundary</name>
  <files>app/[locale]/layout.tsx</files>
  <action>
Import ErrorBoundary and wrap the children in LocaleLayout.

Placement: Wrap inside the body element, around the TranslationProvider and children.
This catches errors from:
- TranslationProvider initialization failures
- Any child component errors
- Page rendering errors

Do NOT wrap the html/body elements themselves - only the content.

Pattern:
```tsx
<body>
  <ErrorBoundary>
    <TranslationProvider locale={locale}>
      {children}
    </TranslationProvider>
  </ErrorBoundary>
</body>
```
  </action>
  <verify>App loads normally at http://localhost:3000, no console errors</verify>
  <done>Layout wraps children with ErrorBoundary, app functions normally</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without errors
- [ ] App loads normally at localhost:3000
- [ ] ErrorBoundary component has proper TypeScript types
- [ ] Fallback UI is styled and readable
</verification>

<success_criteria>
- ErrorBoundary component created with class component pattern
- App layout wraps content with ErrorBoundary
- Fallback UI shows on error (can test by temporarily throwing in a component)
- No TypeScript errors
- App functions normally when no errors occur
</success_criteria>

<output>
After completion, create `.planning/phases/07-error-handling/07-01-SUMMARY.md`
</output>
